{
  "version": 2,
  "builds": [
    {
      "src": "src/routes/**/*.ts",
      "use": "@vercel/node",
      "config": {
        "includeFiles": ["src/**"]
      }
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "src/routes/$1"
    }
  ]
}
